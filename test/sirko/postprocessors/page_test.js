import assert from 'assert';

import Page from '../../../src/sirko/postprocessors/page';

describe('Page', function() {
 // there is no reliable way to test this module
 // we can try to stub the Cache#add method, but it might be a bad idea
 // since we don't own it
});
